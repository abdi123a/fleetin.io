# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deploy target config — host and paths, not secrets, but machine-specific.
deploy/deploy.env

# Vendored third-party agent tooling (bundled fonts + icon/font datasets).
# Local tooling, not app code — see .claude/launch.json which IS tracked.
.claude/skills/
