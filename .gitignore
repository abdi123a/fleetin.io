# Dependencies
node_modules/

# Build output
dist/
build/
coverage/

# Secrets — never commit. Use .env.example as the documented template.
.env
.env.*
!.env.example

# Local uploads written by StorageService's local driver
uploads/

# Logs
*.log
logs/

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp

# Deploy target config — host and paths, not secrets, but machine-specific.
deploy/deploy.env

# Local database dumps written before migrations (deploy/deploy.sh, manual pg/mysqldump).
# Snapshots of real data — large and never belong in the repo.
backups/
.backups/
