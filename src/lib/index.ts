/**
 * Third-party library configuration.
 *
 * The line against `utils/`: modules here configure or adapt an external
 * dependency (TanStack Query, an HTTP client, a date locale); `utils/` holds
 * dependency-free helpers.
 */
export * from './queryClient';
