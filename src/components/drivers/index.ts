/**
 * The driver profile, shared by the Drivers page and a transporter's dossier.
 * One body, two hosts — see `DriverProfile.tsx` for why.
 */

export {
  DriverExpiryLabel,
  DriverProfileHeader,
  DriverProfileOverview,
  DriverProfileSheet,
  DriverStatusPill,
  type DriverProfileHeaderProps,
  type DriverProfileOverviewProps,
  type DriverProfileSheetProps,
} from './DriverProfile';
