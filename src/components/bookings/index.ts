/**
 * Booking-level shared UI. The debrief lives here because two different status
 * controls ask it — see `BookingDebriefDialog.tsx`.
 */

export {
  BookingDebriefDialog,
  DEBRIEF_SUBJECTS,
  debriefSubjectFor,
  emptyDebrief,
  type BookingDebriefDialogProps,
  type DebriefDraft,
  type DebriefSubject,
} from './BookingDebriefDialog';
