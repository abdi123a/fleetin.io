/**
 * The chrome every record side panel shares. See `PanelHeader.tsx` for why the
 * four panels stopped each inventing their own.
 */

export { PanelHeader, type PanelHeaderProps } from './PanelHeader';
