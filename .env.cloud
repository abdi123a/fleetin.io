# Mode: `vite --mode cloud` (npm run dev:cloud)
#
# Points the app at the shared hosted backend. Use it to reproduce something a
# colleague sees, or when the local API is not running.
#
# This is real shared data — writes from here are not sandboxed.
VITE_API_BASE_URL=https://api.fleetin.hammi.cloud/api/v1
